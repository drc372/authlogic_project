# User Account Baseline

This application is a user account and login system for Ruby on Rails (RoR). The code here will serve as a baseline for future web applications.  This is intended to be a starting point every time we pivot.

ManInTheVan