<h1>users/index.html.erb</h1>
<div id= "user_nav">
<%= link_to "Home", root_path %>
<%= link_to "Logout", logout_path %>
</div>

<p> Users page </p>
<p> maybe just redirect </p>
<p> You are logged in as <%= "#{current_user.login}" %> </p>

<%= debug(params) if Rails.env.development? %>
