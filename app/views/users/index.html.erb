<h1>users/index.html.erb</h1>
<div id= "user_nav">
<%= link_to "Home", root_path %>
<% if @userlevel > 5 %>
  <%= link_to "Logout", logout_path %>
<% end %>
</div>

<p> Users page </p>
<p> maybe this can be the search page for all users </p>
<% if current_user %>
<p> You are logged in as <%= "#{current_user.login}" %> </p>
<% end %>
<%= debug(params) if Rails.env.development? %>
