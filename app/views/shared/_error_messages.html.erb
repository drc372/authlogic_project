<% if target.error_messages.any? %>
   <div id="errorExplanation">
     <h2><%= pluralize(target.error_messages.count, "error") %>  prohibited this record from being saved: !></h2>
      	<ul>  
      	      <% target.error_messages.full_messages.each do |msg| %>  
              	 <li><%= msg %></li>  
      	      <% end %>  
      	</ul>  
   </div>
<% else %>
   No errors!
<% end %> 
